Brethink

App logic:

- app state

can go to:

- x4 firmware (the actual hardware abstraction)
- wasm target whichi instead of firmware targets the html canvas
- tests to emulate fake firmware for automated tests
